
name: Build Unicorn APK
on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4 # Yangilandi

      - name: Setup Node.js
        uses: actions/setup-node@v4 # Yangilandi
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install

      - name: Build Next.js Web
        run: npm run build
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}

      - name: Setup Java
        uses: actions/setup-java@v4 # Yangilandi
        with:
          distribution: 'zulu'
          java-version: '17' # Professional applar uchun Java 17 barqaror

      - name: Build Android APK
        run: |
          npx cap sync
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload Artifact
        uses: actions/upload-artifact@v4 # XATONI TUZATISH: v3 o'rniga v4 qo'yildi
        with:
          name: KidUply-Unicorn-Build
          path: android/app/build/outputs/apk/debug/app-debug.apk
